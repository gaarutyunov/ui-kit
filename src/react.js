/* =========================================================================
   `@gaarutyunov/ui-kit/react` — types-only entry.

   This module has no runtime behaviour; its purpose is the accompanying
   `react.d.ts`, which augments `React.JSX.IntrinsicElements` with every
   `ga-*` element. Import it once for the side effect of pulling those types
   into your project, or reference the types directly:

     import "@gaarutyunov/ui-kit/react";
     // or
     /// <reference types="@gaarutyunov/ui-kit/react" />

   Registering the actual custom elements is still done from the main entry:
     import "@gaarutyunov/ui-kit";
   ========================================================================= */
export {};
